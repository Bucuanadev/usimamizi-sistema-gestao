[providers]
  node = "18"

[phases.build]
  cmds = [
    "npm install",
    "cd backend && npm install",
    "cd .. && npm run build"
  ]

[phases.start]
  cmd = "node backend/server.js"

[variables]
  NODE_ENV = "production"
  PORT = "3001"